'use strict'
// Минимум. Напишите функцию min, принимающую два аргумента, и возвращающую минимальный из них.

// console.log(min(0, 10));
// // → 0
// console.log(min(0, -10));
// // → -10
let min = function(el1, el2){
  if(el1>el2){
    return el2
  }else{
    return el2
  } 
}